---
layout: page.njk
title: "Location-Specific Adoption Resources"
description: "Find adoption resources and support specific to your state or location. From Utah to Texas, Florida to Ohio, we provide comprehensive adoption services nationwide."
image: "/assets/images/Logo.jpg"
imageAlt: "Location-specific adoption resources and support"
permalink: "/location-blog/"
---

<style>
.location-blog-header {
	text-align: center;
	padding: 3rem 20px;
	background: linear-gradient(135deg, #f5bf50 0%, #ffd700 100%);
	margin-bottom: 3rem;
	margin-top: 2rem;
	border-radius: 20px;
	box-shadow: 0 8px 25px rgba(245, 191, 80, 0.3);
}

.location-blog-header h1 {
	color: #2c3e50;
	font-size: 2.5rem;
	margin-bottom: 1rem;
	font-weight: 700;
}

.location-blog-header p {
	color: #2c3e50;
	font-size: 1.2rem;
	max-width: 800px;
	margin: 0 auto;
	line-height: 1.6;
}

.location-filters {
	display: flex;
	flex-wrap: wrap;
	justify-content: flex-start;
	align-items: flex-start;
	gap: 0.3rem 0.6rem;
	margin-bottom: 2rem;
	padding: 0.5rem 0.2rem;
	background: #fffbe8;
	border-radius: 18px;
	box-shadow: 0 2px 8px rgba(245,191,80,0.08);
	width: 100%;
	max-width: 100vw;
}

.location-filters::-webkit-scrollbar {
	height: 6px;
	background: #fffbe8;
}
.location-filters::-webkit-scrollbar-thumb {
	background: #F5BF50;
	border-radius: 6px;
}

.location-filter {
	display: inline-block;
	padding: 0.45rem 1rem;
	background: #ffffff;
	border: 2px solid #F5BF50;
	color: #2c3e50;
	border-radius: 25px;
	cursor: pointer;
	transition: all 0.3s ease;
	font-weight: 600;
	font-size: 1rem;
	text-decoration: none;
	margin-bottom: 0;
	white-space: nowrap;
	box-shadow: 0 1px 3px rgba(245,191,80,0.04);
	margin-top: 0.3rem;
	max-width: 140px;
	min-width: 90px;
	text-align: center;
}

/* Brick effect: stagger every other filter in a row */
.location-filter:nth-child(2n) {
	margin-top: -0.2rem;
}

.location-filter:hover,
.location-filter.active {
	background: #F5BF50;
	color: #2c3e50;
	box-shadow: 0 4px 15px rgba(245, 191, 80, 0.3);
}

.location-grid {
	display: grid;
	grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
	gap: 2rem;
	margin-bottom: 3rem;
}

.location-post {
	background: #ffffff;
	border-radius: 15px;
	overflow: hidden;
	box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
	transition: transform 0.3s ease, box-shadow 0.3s ease;
	border: 1px solid rgba(245, 191, 80, 0.1);
}

.location-post:hover {
	transform: translateY(-5px);
	box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
}

.location-post img {
	width: 100%;
	height: 200px;
	object-fit: cover;
}

.location-post-content {
	padding: 1.5rem;
}

.location-post-date {
	font-size: 0.9rem;
	color: #6c757d;
	margin-bottom: 0.5rem;
}

.location-post-title {
	font-size: 1.3rem;
	color: #2c3e50;
	margin-bottom: 1rem;
	font-weight: 600;
	line-height: 1.4;
}

.location-post-title a {
	color: inherit;
	text-decoration: none;
}

.location-post-title a:hover {
	color: #F5BF50;
}

.location-post-excerpt {
	color: #6c757d;
	margin-bottom: 1rem;
	line-height: 1.6;
}

.location-post-location {
	display: inline-block;
	background: #f8e6b3;
	color: #2c3e50;
	padding: 0.3rem 0.8rem;
	border-radius: 15px;
	font-size: 0.8rem;
	font-weight: 600;
	margin-bottom: 1rem;
}

.read-more-link {
    color: black;
    text-decoration: none;
    font-weight: 600;
    font-size: 0.9rem;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
}

.read-more-link:hover {
	text-decoration: underline;
}

.no-posts {
	text-align: center;
	padding: 3rem;
	color: #6c757d;
	font-size: 1.1rem;
}

@media (max-width: 768px) {
	.location-filters {
		flex-direction: row;
		align-items: center;
	}
}

@media (max-width: 600px) {
	.location-filter {
		display: block;
		width: 90px;
		min-width: 80px;
		margin-bottom: 0.6rem;
		font-size: 0.93rem;
		padding: 0.4rem 0.5rem;
		border-radius: 25px;
		box-shadow: 0 1px 3px rgba(245, 191, 80, 0.04);
		border: 1.5px solid #F5BF50;
		background: #fff;
		break-inside: avoid;
		text-align: center;
	}
}

@media (min-width: 601px) {
	.location-filters {
		display: flex;
		flex-wrap: wrap;
		justify-content: center;
		gap: 0.5rem;
		margin-bottom: 2rem;
		padding: 0.5rem 0;
		background: #fffbe8;
		border-radius: 18px;
		box-shadow: 0 2px 8px rgba(245,191,80,0.08);
	}
	.location-filter {
		display: inline-block;
		font-size: 1rem;
		padding: 0.5rem 1rem;
		margin-bottom: 0.5rem;
		border-radius: 25px;
		max-width: 140px;
		min-width: 90px;
		text-align: center;
	}
}

@media (max-width: 768px) {
	.location-blog-header h1 {
		font-size: 2rem;
	}
	
	.location-filters {
		grid-template-columns: 1fr;
	}
	
	.location-grid {
		grid-template-columns: 1fr;
	}
}
</style>

<div class="location-blog-header">
	<h1>Location-Specific Adoption Resources</h1>
	<p>Find adoption support and resources tailored to your state or location. Whether you're in Utah, Texas, Florida, Ohio, or anywhere else, we provide comprehensive adoption services nationwide.</p>
</div>

<div class="location-filters">
	<a href="#" class="location-filter active" data-location="all">All Locations</a>
	<a href="#" class="location-filter" data-location="alabama">Alabama</a>
	<a href="#" class="location-filter" data-location="alaska">Alaska</a>
	<a href="#" class="location-filter" data-location="arizona">Arizona</a>
	<a href="#" class="location-filter" data-location="arkansas">Arkansas</a>
	<a href="#" class="location-filter" data-location="colorado">Colorado</a>
	<a href="#" class="location-filter" data-location="florida">Florida</a>
	<a href="#" class="location-filter" data-location="idaho">Idaho</a>
	<a href="#" class="location-filter" data-location="illinois">Illinois</a>
	<a href="#" class="location-filter" data-location="kansas">Kansas</a>
	<a href="#" class="location-filter" data-location="louisiana">Louisiana</a>
	<a href="#" class="location-filter" data-location="mississippi">Mississippi</a>
	<a href="#" class="location-filter" data-location="missouri">Missouri</a>
	<a href="#" class="location-filter" data-location="montana">Montana</a>
	<a href="#" class="location-filter" data-location="nevada">Nevada</a>
	<a href="#" class="location-filter" data-location="new-mexico">New Mexico</a>
	<a href="#" class="location-filter" data-location="ohio">Ohio</a>
	<a href="#" class="location-filter" data-location="west-virginia">West Virginia</a>
	<a href="#" class="location-filter" data-location="wyoming">Wyoming</a>
</div>

{% set stateNames = ['alabama', 'alaska', 'arizona', 'arkansas', 'colorado', 'florida', 'idaho', 'illinois', 'kansas', 'louisiana', 'mississippi', 'missouri', 'montana', 'nevada', 'new-mexico', 'ohio', 'west-virginia', 'wyoming'] %}

<div class="location-grid" id="location-posts">
	{% for post in collections.locationSpecific | shuffle %}
	<article class="location-post" data-locations="{% for tag in post.data.tags %}{% if stateNames.includes(tag) %}{{ tag }}{% endif %}{% endfor %}">
		<a href="{{ post.url }}">
			<img src="{{ post.data.thumbnail }}" alt="{{ post.data.title }}">
		</a>
		<div class="location-post-content">
			<div class="location-post-date">
				{{ post.data.date | customDateFormat("medium") }}
			</div>
			<h3 class="location-post-title">
				<a href="{{ post.url }}">{{ post.data.title }}</a>
			</h3>
			<p class="location-post-excerpt">
				{{ post.data.excerpt | truncate(150) }}
			</p>
			{% for tag in post.data.tags %}
			{% if stateNames.includes(tag) %}
			<span class="location-post-location">{{ tag | title }}</span>
			{% endif %}
			{% endfor %}
			<a href="{{ post.url }}" class="read-more-link">Read Full Article →</a>
		</div>
	</article>
	{% endfor %}
</div>

<div class="no-posts" id="no-posts" style="display: none;">
	<p>No posts found for this location. Check back soon for more resources!</p>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
	const filters = document.querySelectorAll('.location-filter');
	const posts = document.querySelectorAll('.location-post');
	const noPosts = document.getElementById('no-posts');
	
	filters.forEach(filter => {
		filter.addEventListener('click', function(e) {
			e.preventDefault();
			
			// Remove active class from all filters
			filters.forEach(f => f.classList.remove('active'));
			
			// Add active class to clicked filter
			this.classList.add('active');
			
			const selectedLocation = this.getAttribute('data-location');
			
			let visiblePosts = 0;
			
			posts.forEach(post => {
				const postLocations = post.getAttribute('data-locations');
				
				if (selectedLocation === 'all' || postLocations.includes(selectedLocation)) {
					post.style.display = 'block';
					visiblePosts++;
				} else {
					post.style.display = 'none';
				}
			});
			
			// Show/hide no posts message
			if (visiblePosts === 0) {
				noPosts.style.display = 'block';
			} else {
				noPosts.style.display = 'none';
			}
		});
	});
});
</script> 